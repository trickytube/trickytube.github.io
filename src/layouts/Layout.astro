---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { siteConfig } from "../config/site";
const GA_ID = import.meta.env.PUBLIC_GA_ID;

interface Props {
  title: string;
}

const { title } = Astro.props;
const name = siteConfig.name;
const taglink = `https://www.googletagmanager.com/gtag/js?id=${GA_ID}`
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <!-- Google tag (gtag.js) -->
{
  GA_ID && import.meta.env.PROD &&(
    <>
      <!-- Google tag (gtag.js) -->
      <script is:inline type="text/partytown" src={taglink}></script>
      <script is:inline type="text/partytown" define:vars={{GA_ID}}>
        window.dataLayer = window.dataLayer || [];
        function gtag() {
          dataLayer.push(arguments);
        }
        gtag('js', new Date());

        gtag('config', GA_ID);
      </script>
    </>
  )
}
    <title>{title} | {name}</title>
  </head>
  <body class="flex flex-col min-h-screen font-display font-normal px-4 max-w-7xl mx-auto bg-white text-black ">
      <Header />
      <main class="flex flex-col flex-1">
        <slot />
      </main>
      <Footer />
  </body>
</html>

